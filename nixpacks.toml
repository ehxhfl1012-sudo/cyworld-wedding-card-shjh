# Railway 빌드 설정 (Maven + Java 17)
[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -jar target/cyworld-wedding-card-shjh-0.0.1-SNAPSHOT.jar"
